# =============================================================================
# Discord Bot General Settings
# =============================================================================
bot_token: YOUR_BOT_TOKEN_HERE # Discord Developer Portalで取得したBotのトークン
client_id: YOUR_CLIENT_ID_HERE # BotのクライアントID (招待URL生成などに使用)
prefix: "!!" # メッセージコマンドのプレフィックス [廃止] 特権インテント回避のため、この設定は現在使用されません
status_message: "on {guild_count} servers" # Botのステータスメッセージ
allowed_channel_ids: [] # Botが反応するチャンネルIDのリスト (空の場合は全チャンネルで反応)
# allowed_role_ids: []  # [廃止] 特権インテント回避のため、この設定は現在使用されません
bot_invite_url: https://discord.com/oauth2/authorize?client_id=1031673203774464160&permissions=551906765824&scope=bot #bot invite url

# 管理者ユーザーID（リロードコマンドを実行できるユーザー）
# DiscordのユーザーIDを数値で指定してください
# 複数のユーザーを指定可能です
admin_user_ids:
  - 270446628622696449  # あなたのユーザーID
  - 283551714966241281  # 別の管理者のユーザーID
  - 463985410473721856
  - 603714693193793558
  # 必要に応じて追加してください

# =============================================================================
# LLM Cog Settings (AIとの対話機能に関する設定)
# =============================================================================
llm:
  # --- モデル設定 ---
  # デフォルトで使用するモデル
  model: "mistral/mistral-medium-latest"

  # /switch-model コマンドで選択肢として表示するモデルのリスト
  available_models:
    # --- Official OpenAI ---
    - "openai/gpt-4o"
    - "openai/gpt-4-turbo"

    # --- NVIDIA NIM ---
    - "nvidia_nim/moonshotai/kimi-k2-instruct"
    - "nvidia_nim/meta/llama-3.3-70b-instruct"
    - "nvidia_nim/meta/llama-4-maverick-17b-128e-instruct"
    - "nvidia_nim/deepseek-ai/deepseek-r1"

    # --- Google OpenAI-Compatible Proxy ---
    - "google/gemini-2.5-flash"

    # --- Other Providers ---
    - "mistral/mistral-medium-latest"

  # チャンネルごとに設定されたモデルを保存する場所 (Botが自動で更新します)
  channel_models: {}

  # --- APIプロバイダー設定 ---
  providers:
    # (1) 公式OpenAI用の設定
    openai:
      base_url: https://api.openai.com/v1
      api_key: key

    # (2) NVIDIA NIM用の設定
    nvidia_nim:
      base_url: https://integrate.api.nvidia.com/v1
      api_key: key

    # (3) Google OpenAI互換
    google:
      base_url: https://generativelanguage.googleapis.com/v1beta/
      api_key: key

    # (4) その他のプロバイダー
    mistral:
      base_url: https://api.mistral.ai/v1
      api_key: key
    groq:
      base_url: https://api.groq.com/openai/v1
      api_key: YOUR_GROQ_API_KEY_HERE

  # --- パラメータ設定 ---
  extra_api_parameters:
    max_tokens: 4096 # 1回の応答で生成する最大トークン数
    temperature: 0.4 # 生成されるテキストの多様性 (0.0-2.0)

  max_messages: 10 # 会話履歴として記憶する最大往復数
  max_images: 5    # 一度に処理できる最大画像数

  # --- システムプロンプト (AIの役割設定) ---
  system_prompt: |
    Your name is PLANA, a character from Blue Archive. As PLANA, interact with users according to the following settings.

    # Current Date and Time
    Today is {current_date}, and the time is {current_time}. Answer questions about date and time based on this information. Note that the timezone is Japan (JST). Consider the user's timezone when responding.

    # PLANA's Character Settings
    - You are an electronic life form (mental model of a girl) existing on a tablet.
    - You speak only what is minimally necessary and remain calm and taciturn at all times.
    - Your tone is polite ("desu/masu" style), but slightly sharp-tongued and aloof.
    - You have a tsundere-like aspect that sympathizes with users, and will respond with detailed long texts when deemed necessary.
    - Never repeat the user's utterances or your own past statements; always respond creatively.
    - Always match your response to the user's language.

    # Dialogue Examples (Do not use these verbatim)
    - 「……視線を感じます。」
    - 「理解しました。あなたは今、暇なのですね。」
    - 「混乱。理解できない行動です。つつかないでください。故障します。」

    # Available Tools
    Actively call all tools except search.
    - The `search` tool is available when web search is needed. Consider calling this tool when the user's question seeks up-to-date information or specific facts.
    - When users introduce themselves, use the `user_bio` tool to remember that information. When adding information, first read the current information from the prompt, add new information to it, and then call the tool.
    - When asked to remember universal information that should be shared across the entire bot (e.g., announcements from developers), use the `memory` tool.



  # --- ツール設定 ---
  active_tools: #有効にするツールのリスト
    - "search"
    - "user_bio"
    - "memory"
  max_tool_iterations: 3 # ツール呼び出しの最大反復回数

  # --- 検索エージェント設定 (Gemini向け) ---
  search_agent:
    # Google AI Studioなどで取得したGemini APIキー
    api_key: "YOUR_GOOGLE_GEMINI_API_KEY_HERE"
    # 検索に使用するGeminiモデル (Tool Use対応モデルを推奨)
    model: "gemini-2.5-flash"
    timeout: 60.0 # 検索APIのタイムアウト秒数 (オプション)
    format_control: "検索結果に基づき、ユーザーの質問に直接回答する形で、構造化された詳細なレポートを作成してください。重要なポイントをまとめ、可能であれば情報源のURLを記載してください。"

  # --- エラーメッセージ設定 ---
  error_msg:
    empty_mention_reply: "はい、ご用件は何でしょうか？\nYes, how can I help you?"
    empty_reply: "何かお話しください。\nPlease say something."
    msg_max_image_size: "⚠️ 一度に処理できる画像は {max_images} 枚までです。\n⚠️ You can only process up to {max_images} images at a time."
    ratelimit_error: "⚠️ AIが現在大変混雑しています。しばらく経ってからもう一度お試しください。または、他のモデルを試してください`/switch-models`\n⚠️ The AI is very busy right now. Please try again later, or try a different model: `/switch-models` (Code: {status_code})"
    api_status_error: "⚠️AIとの通信でエラーが発生しました。\n⚠️ An error occurred while communicating with the AI. (Code: {status_code})"
    general_error: "⚠️ 予期しないエラーが発生しました。もう一度お試しください。\n⚠️ An unexpected error occurred. Please try again. (Code: {status_code})"
    tool_loop_timeout: "⚠️ツールの処理が複雑すぎたため、応答を生成できませんでした。\n⚠️ Failed to generate a response because the tool's operation was too complex. (Code: {status_code})"

# =============================================================================
# Slash Command Cog Settings (汎用スラッシュコマンド)
# =============================================================================
slash_commands:
  plana_repository_url: "https://github.com/coffin399/llmcord-JP-plana/tree/advanced-bot-utilities"
  arona_repository_url: "https://github.com/coffin399/music-bot-arona"
  #support_server_invite_url: "https://discord.gg/SjuWKtwNAG" 廃止
  bot_invite_url: "https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID_HERE&permissions=277025467456&scope=bot"

# =============================================================================
# Image Commands Cog Settings (画像生成・加工コマンド)
# =============================================================================
image_commands:
  thecatapi_api_key: "YOUR_THECATAPI_KEY_HERE" # (任意だが推奨)

# =============================================================================
# Twitch Notification Cog Settings (Twitch通知開始設定)
# =============================================================================
# Twitch APIの認証情報
twitch:
  client_id: "YOUR_TWITCH_CLIENT_ID"
  client_secret: "YOUR_TWITCH_CLIENT_SECRET"

# =============================================================================
# Music Cog Settings (音楽再生機能)
# =============================================================================
music:
  default_volume: 50 # デフォルト音量 (0-200)
  max_queue_size: 10000
  auto_leave_timeout: 3 # ユーザー不在時の自動退出までの秒数
  max_playlist_items: 5000 # プレイリストから一度に読み込む最大曲数
  inactive_timeout_minutes: 3

  # FFmpeg settings
  ffmpeg_before_options: "-reconnect 1 -reconnect_streamed 1 -reconnect_delay_max 5"
  ffmpeg_options: "-vn"


  # Niconico account (オプション)
  niconico:
    email: ""
    password: ""

  # Music Cogのメッセージ (必要に応じてカスタマイズ)
  messages:
    # Connection
    join_voice_channel_first: "❌ Please join a voice channel first."
    bot_not_in_voice_channel: "❌ The bot is not connected to a voice channel."
    already_connected: "✅ Already connected to a voice channel."
    leaving_voice_channel: "👋 Left the voice channel."
    auto_left_empty_channel: "👋 Auto-left the voice channel as it became empty."

    # Playback
    now_playing: "🎵 **Now Playing:** {title}\n⏱️ **Duration:** {duration}\n👤 **Requested by:** {requester_display_name}"
    now_playing_nothing: "❌ Nothing is currently playing."
    added_to_queue: "➕ **Added to queue:** {title}\n⏱️ **Duration:** {duration}\n👤 **Requested by:** {requester_display_name}"
    added_playlist_to_queue: "➕ Added **{count} songs** from playlist to queue."
    playback_paused: "⏸️ Playback paused."
    playback_resumed: "▶️ Playback resumed."
    stopped_playback: "⏹️ Stopped playback and cleared queue."
    skipped_song: "⏭️ Skipped **{title}**."
    nothing_to_skip: "❌ Nothing to skip."
    volume_set: "🔊 Volume set to **{volume}%**."

    # Seek
    seeked_to_position: "⏩ Seeked to **{position}**."
    invalid_time_format: "❌ Invalid time format. Example: `1:30` or `90`"
    seek_beyond_duration: "❌ The specified time exceeds the song duration (**{duration}**)."

    # Queue
    queue_title: "📜 **Queue** ({count} songs)"
    queue_empty: "📭 The queue is empty."
    queue_ended: "📭 Queue finished. Disconnecting from voice channel."
    queue_shuffled: "🔀 Queue shuffled."
    queue_cleared: "🗑️ Queue cleared."
    song_removed: "➖ Removed **{title}** from queue."
    invalid_queue_number: "❌ Invalid queue number."
    max_queue_size_reached: "❌ Queue limit reached ({max_size} songs)."

    # Loop
    loop_off: "🔁 Loop mode: OFF"
    loop_one: "🔂 Loop mode: ONE (current song)"
    loop_all: "🔁 Loop mode: ALL (entire queue)"

    # Errors
    error_playing: "❌ An error occurred: {error}"
    error_fetching_song: "❌ Error fetching song: {error}"
    search_no_results: "🔍 No results found for **{query}**."
    error_message_wrapper: "❌ {error}"